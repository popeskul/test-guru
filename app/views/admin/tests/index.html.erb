<h1><%= t('.header') %></h1>

<table>
  <thead>
  <tr>
    <th><%= t('helpers.label.test.title') %></th>
    <th><%= t('helpers.label.test.level') %></th>
    <th><%= t('helpers.label.test.questions') %></th>
    <th colspan="3"><%= t('helpers.label.test.action') %></th>
  </tr>
  </thead>
  <tbody>
  <%= render @tests %>
  </tbody>
</table>

<br />

<%= link_to t('helpers.submit.test.create'), new_admin_test_path %>
