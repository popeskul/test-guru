<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="flash alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>">
      <%= content_tag :div, msg, id: "warning: #{name}" %>
    </div>
  <% end %>
<% end %>
