<nav class="navbar navbar-expand-lg navbar-light  <%= current_user&.admin? ? 'text-white bg-primary' : '' %>">
  <div class="container">
    <%= link_to 'TestGuru', current_user&.admin? ? admin_tests_path : root_path, class: current_user&.admin? ? 'navbar-brand text-white' : 'navbar-brand' %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item"><b><%= welcome_message(current_user) %></b></li>
          <li class="nav-item"><%= current_user.email %></li>
          <li><%= link_to 'Badges', badges_path, class: 'badge bg-dark text-light' %></li>
        <% end %>
      </ul>

      <% if user_signed_in? %>
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to t('tests.index.header'), tests_path, class: current_user&.admin? ? 'nav-link text-white' : 'nav-link' %></li>
          <li class="nav-item"><%= link_to t('.exit'), destroy_user_session_path, method: :delete, class: current_user&.admin? ? 'nav-link text-white' : 'nav-link' %></li>
          <li><%= select_language %></li>
        </ul>
      <% else %>
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to t('.sign_up'), new_user_registration_path, class: current_user&.admin? ? 'nav-link text-white' : 'nav-link' %></li>
          <li class="nav-item"><%= link_to t('.sign_in'), new_user_session_path, class: current_user&.admin? ? 'nav-link text-white' : 'nav-link' %></li>
          <li><%= select_language %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
